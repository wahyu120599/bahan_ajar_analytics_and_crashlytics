import 'package:firebase_analytics/firebase_analytics.dart';

class Analitic {
  FirebaseAnalytics analitic = FirebaseAnalytics.instance;
  static logeven(eventname, params) {
    Analitic.logeven(eventname, params);
  }
}
